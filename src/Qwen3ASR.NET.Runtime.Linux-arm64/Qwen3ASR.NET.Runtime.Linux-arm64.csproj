<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>

    <!-- Package metadata -->
    <PackageId>Qwen3ASR.NET.Runtime.Linux-arm64</PackageId>
    <Description>Native runtime for Qwen3ASR.NET (Linux ARM64/aarch64, CPU only - e.g. Raspberry Pi 5, Rockchip RK3588)</Description>
    <PackageTags>speech-recognition;asr;qwen;ai;ml;transcription;native;runtime;arm64;aarch64;cpu</PackageTags>
    <PackageReadmeFile>README.md</PackageReadmeFile>

    <!-- Primarily ships native binaries; a managed placeholder assembly is included
         so NuGet accepts this as a TFM-targeted package (avoids NU5128/NU5017). -->
    <GenerateAssemblyInfo>false</GenerateAssemblyInfo>
  </PropertyGroup>

  <ItemGroup>
    <None Include="runtimes\**\*" Pack="true" PackagePath="runtimes" />
    <Content Include="runtimes\linux-arm64\native\libqwen3_asr_ffi.so" CopyToOutputDirectory="PreserveNewest" />
    <None Include="..\..\README.md" Pack="true" PackagePath="" />
  </ItemGroup>

</Project>
