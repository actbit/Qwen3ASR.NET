<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>

    <!-- Package metadata -->
    <PackageId>Qwen3ASR.NET.Runtime.Linux-x64</PackageId>
    <Description>Native runtime for Qwen3ASR.NET (Linux x64 with CUDA support)</Description>
    <PackageTags>speech-recognition;asr;qwen;ai;ml;transcription;native;runtime;cuda</PackageTags>
    <PackageReadmeFile>README.md</PackageReadmeFile>

    <!-- Primarily ships native binaries; a managed placeholder assembly is included
         so NuGet accepts this as a TFM-targeted package (avoids NU5128/NU5017). -->
    <GenerateAssemblyInfo>false</GenerateAssemblyInfo>
  </PropertyGroup>

  <ItemGroup>
    <None Include="runtimes\**\*" Pack="true" PackagePath="runtimes" />
    <Content Include="runtimes\linux-x64\native\libqwen3_asr_ffi.so" CopyToOutputDirectory="PreserveNewest" />
    <None Include="..\..\README.md" Pack="true" PackagePath="" />
  </ItemGroup>

</Project>
